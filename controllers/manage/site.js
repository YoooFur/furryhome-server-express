/**
 * 控制器
 * 站点管理
 * controllers/manage/site.js
 */

// 引入类型
const {Request, Response} = require('express');




module.exports = {

    /**
     * 获取站点列表
     * @param {Response} req 
     * @param {*} res 
     */
    getSiteList: async (req, res) => {
        
    }
}